#include <stdio.h>

int main() {
    char character;
    scanf(" %c", &character);
    if ((character >= 65 && character <= 90) || (character >= 97 && character <= 122))
        printf("Alphabet");
    else if (character >= 48 && character <= 57)
        printf("Digit");
    else
        printf("Symbol");
    return 0;
}
